eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
server:
  port: 8765
spring:
  application:
    name: service-feign

# 开启断路器
feign:
  hystrix:
    enabled: true

